DEF Main():
	integer number;
	boolean isprime;
	integer current;
	
	read(number);
	current = 2; 
	
	for(curr < number ; curr add 1)
	{
		if( number % curr == 0 )
			isprime = false;
	}		
			
	display(isprime);
CLOSE